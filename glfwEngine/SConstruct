src = Split('''
opengl/glfuncs.d
opengl/gltypes.d
opengl/glfw.d
''')

src += Split('''
src/main.d
''')

env = Environment()
env.Program("glfwtest", src, DFLAGS = Split("-unittest -gc -g"), LIBS=["pthread", "rt", "phobos2", "GL", "glfw"])
