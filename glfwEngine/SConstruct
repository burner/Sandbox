src = Split('''
opengl/glfuncs.d
opengl/gltypes.d
opengl/glfw.d
''')

src += Split('''
src/main.d
''')

env = Environment()
env.Program("glfwtest", src, TARGET_ARCH='x86_64', DFLAGS = Split("-m64 -unittest -gc -g"), LIBS=["pthread", "rt", "phobos2", "GL", "glfw"])
